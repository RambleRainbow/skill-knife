# SkillManager - VSCode 插件设计文档

## 概述

SkillManager 是一个 VSCode 插件，用于查看和管理本机上已安装的 AI agent skills（按照 [agentskills.io](https://agentskills.io/home) 定义的标准格式）。

### 核心功能

- 展示当前存在哪些 skills（按全局/项目维度）
- 区分不同 agent 软件的 skill 读取路径（Claude Code、Codex、Gemini CLI 等）
- 从公开 skill market 安装新 skills

### 支持平台

macOS、Linux、Windows（全平台）

---

## 核心概念

### Skill

按照 [agentskills.io](https://agentskills.io/home) 定义的标准格式。

### Scope（作用域）

- **Global（全局）** - 如 `~/.claude/skills/`
- **Project（项目级）** - 如 `./.claude/skills/`

项目级会覆盖全局。

### SkillReader

描述某个 agent 软件如何读取 skills 的配置。

```typescript
interface SkillReader {
  id: string;              // "claude-code" | "codex" | "gemini-cli" | ...
  name: string;            // 显示名称 "Claude Code"
  version?: string;        // 版本号或版本范围，如 "1.x" | ">=2.0"
  globalPath: string;      // 全局 skill 路径，如 "~/.claude/skills"
  projectPath: string;     // 项目 skill 路径，如 ".claude/skills"
}
```

### Market（市场）

一个 git 仓库地址，符合 openskills 协议。预置几个知名市场（如 Anthropic 官方、社区热门），用户可增删。

---

## UI 架构

### 侧边栏树状视图

Skill 作为一级公民，单行展示，位置用行内标签：

```
SKILL MANAGER
├── 📄 brainstorming          [Proj·CC,CX]
├── 📄 systematic-debugging   [Proj·CC]
├── 📄 writing-plans          [Glob·CC,GM]
├── 📄 test-driven-dev        [Proj·CC][Glob·CX]
└── 🛒 Markets (2)
```

标签说明：
- `Proj` = Project, `Glob` = Global
- `CC` = Claude Code, `CX` = Codex, `GM` = Gemini CLI
- 多个位置用多个 `[]` 标签

### Webview 页面

#### 1. 已安装 Skills 总览页

```
┌─────────────────────────────────────────────────────────────────┐
│  Installed Skills (12)                               [Refresh] │
├─────────────────────────────────────────────────────────────────┤
│  Filter: [Scope ▼] [Reader ▼]        Search: [___________]     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📄 brainstorming                                        │   │
│  │ Turn ideas into designs through collaborative dialogue  │   │
│  │ 📍 Project: Claude Code, Codex                          │   │
│  │                              [View] [Delete] [Open Dir] │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📄 systematic-debugging                                 │   │
│  │ Debug issues methodically with root cause analysis      │   │
│  │ 📍 Project: Claude Code  ·  Global: Codex               │   │
│  │                              [View] [Delete] [Open Dir] │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2. Skill 详情页

```
┌─────────────────────────────────────────────────────────────────┐
│  ← Back                                                         │
├─────────────────────────────────────────────────────────────────┤
│  # brainstorming                                                │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ Installed at:                                             │ │
│  │ ├─ Project (skillManage)                                  │ │
│  │ │   └─ Claude Code: .claude/skills/brainstorming/        │ │
│  │ │   └─ Codex: .codex/skills/brainstorming/               │ │
│  │ └─ Global                                                 │ │
│  │     └─ (not installed)                       [+ Install]  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Source: github.com/anthropics/skills                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ## Overview                                                    │
│  Help turn ideas into fully formed designs...                  │
│                                                                 │
│  (rendered SKILL.md content)                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 3. Market 浏览页

```
┌─────────────────────────────────────────────────────────────────┐
│  Skill Markets                                       [Add New] │
├─────────────────────────────────────────────────────────────────┤
│  Market: [Anthropic Official ▼]   Search: [___________]        │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📄 code-review                                          │   │
│  │ Automated code review with best practices               │   │
│  │                                              [Install]  │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📄 brainstorming                            [Installed] │   │
│  │ Turn ideas into designs...                              │   │
│  │                                      [View] [Update]    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

#### 4. 安装对话框

```
┌───────────────────────────────────────┐
│  Install "code-review"                │
├───────────────────────────────────────┤
│  Scope:                               │
│  ● Project (skillManage)              │
│  ○ Global                             │
├───────────────────────────────────────┤
│  Install to:                          │
│  ☑ Claude Code                        │
│  ☑ Codex                              │
│  ☑ Gemini CLI                         │
├───────────────────────────────────────┤
│             [Cancel]  [Install]       │
└───────────────────────────────────────┘
```

---

## 技术架构

### 技术栈

- **VSCode Extension API** - 侧边栏 TreeView、Webview、配置管理
- **TypeScript** - 主语言
- **openskills CLI** - 调用其命令实现市场读取、安装、更新
- **Webview UI** - 可选用轻量框架（如 Lit、Svelte）或纯 HTML/CSS

### 核心模块

```
src/
├── extension.ts              # 插件入口
├── config/
│   └── readers.ts            # SkillReader 默认配置 + 用户配置合并
├── services/
│   ├── skillScanner.ts       # 扫描本地已安装 skills
│   ├── marketService.ts      # 调用 openskills 读取市场
│   └── installService.ts     # 调用 openskills 安装/更新
├── views/
│   ├── sidebarProvider.ts    # 侧边栏 TreeDataProvider
│   └── webviewProvider.ts    # Webview 面板管理
├── webview/                  # Webview 前端代码
│   ├── installed.html
│   ├── detail.html
│   └── market.html
└── types/
    └── index.ts              # Skill, SkillReader, Market 等类型定义
```

### 数据流

```
┌─────────────┐     scan      ┌──────────────┐
│ Local Paths │ ───────────→ │ skillScanner │
└─────────────┘               └──────┬───────┘
                                     │
                                     ▼
┌─────────────┐   openskills  ┌──────────────┐     ┌─────────────┐
│ Git Markets │ ───────────→ │marketService │ ──→ │  Webview UI │
└─────────────┘               └──────────────┘     └─────────────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │installService│ → 写入到对应 Reader 路径
                              └──────────────┘
```

---

## 配置结构

### VSCode settings.json 配置项

```jsonc
{
  // SkillReader 配置（插件内置默认值，用户可覆盖/扩展）
  "skillManager.readers": [
    {
      "id": "claude-code",
      "name": "Claude Code",
      "version": ">=1.0",
      "globalPath": "~/.claude/skills",
      "projectPath": ".claude/skills"
    },
    {
      "id": "codex",
      "name": "Codex",
      "globalPath": "~/.codex/skills",
      "projectPath": ".codex/skills"
    }
  ],

  // 市场配置（预置 + 用户可增删）
  "skillManager.markets": [
    {
      "name": "Anthropic Official",
      "git": "https://github.com/anthropics/skills.git"
    },
    {
      "name": "Community Skills",
      "git": "https://github.com/openskills-community/skills.git"
    }
  ],

  // 安装默认值
  "skillManager.install.defaultScope": "project",
  "skillManager.install.defaultReaders": ["all"]
}
```

### 配置合并策略

- `readers`: 用户配置与内置配置按 `id` 合并，用户可覆盖内置项
- `markets`: 用户配置完全替换内置预置（如需保留预置需手动包含）

---

## 里程碑

### 里程碑 1（MVP）

| 功能 | 说明 |
|------|------|
| 扫描已安装 skills | 根据配置的 readers 扫描全局/项目路径 |
| 侧边栏展示 | Skill 列表 + 位置标签 |
| Skill 详情页 | 渲染 SKILL.md、显示安装位置 |
| Reader 配置 | 内置默认 readers，用户可在 settings.json 覆盖 |

### 里程碑 2

| 功能 | 说明 |
|------|------|
| Market 浏览 | 调用 openskills 读取 git 市场 |
| 安装 skill | 选择 scope + readers，调用 openskills 安装 |
| Market 配置 | 预置市场 + 用户可在 settings.json 增删 |

### 里程碑 3

| 功能 | 说明 |
|------|------|
| 更新检测 | 对比已安装与市场版本 |
| 批量操作 | 多选安装/删除/更新 |
| 搜索过滤 | 按名称、描述搜索 |

### 不做

| 功能 | 原因 |
|------|------|
| Skill 编辑/创建 | 超出"管理"范围，用户可直接编辑文件 |
| Skill 执行/测试 | 属于各 agent 软件职责 |
